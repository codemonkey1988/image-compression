CREATE TABLE sys_file_metadata (
	image_compression_last_compressed int(11) DEFAULT '0' NOT NULL
);

CREATE TABLE sys_file_processedfile (
	image_compression_last_compressed int(11) DEFAULT '0' NOT NULL
);